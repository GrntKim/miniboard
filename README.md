# Micro CMS

Single-file CMS experiment with PHP, jQuery, and SQLite.

## Run

```bash
php -S localhost:8000
```

Open `http://localhost:8000`.

## Routes

- `/`
- `/?action=posts`
- `/?action=write`

`db.sqlite` is created automatically and should stay out of git.
